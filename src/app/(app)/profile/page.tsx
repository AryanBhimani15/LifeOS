export const metadata = { title: "LifeOS — Profile" };

/** Reserved route for the profile experience. */
export default function ProfilePage() {
  return <main className="profile-placeholder" aria-label="Profile" />;
}
